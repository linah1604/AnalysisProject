/*
assumptions:
each order has a fixed deadline day without time
a day has 24 hours hence number of hours till order is days_left * 24
a worker cannot work on two orders simultaneously
theres a days vector that is the size of the longest deadline_days_left and 
each day the var deadline_days_left for each order is decremented by one
that we're measuring per worker hours per day and not time blocking
there are no delays and workers don't have breaks

constraints:
completetion_time < deadline_days_left * 24
worker.assigned_hours < worker.working_hours
worker.total_hours < reg_hours + overtime_hours


*/